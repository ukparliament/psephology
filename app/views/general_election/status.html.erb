<table>
	<thead>
		<tr>
			<%= content_tag( 'td', 'Election state' ) %>
			<%= content_tag( 'td', 'Constituency election count', :class => 'number' ) %>
		</tr>
	</thead>
	<tbody>
		<%= render :partial => 'state', :collection => @election_states %>
	</tbody>
</table>