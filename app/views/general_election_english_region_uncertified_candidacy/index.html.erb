<%= render :partial => 'previous_next', :object => @general_election %>

<%= render :partial => 'general_election_english_region/nav' %>

<%= render :partial => 'general_election/counts', :object => @general_election %>

<% unless @uncertified_candidacies.empty? -%>

	<%= content_tag( 'p', "Candidates standing in the general election on #{@general_election.polling_on.strftime( $DATE_DISPLAY_FORMAT )} who were not nominated by a political party." ) %>
	<div class="table-wrapper">
		<table id="results" class="sortable sorted-<%= @order %>">
			<%= render :partial => 'table_head' %>
			<tbody>
				<%= render :partial => 'general_election_uncertified_candidacy/uncertified_candidacy', :collection => @uncertified_candidacies %>
			</tbody>
		</table>
	</div>
	<div id="downloads">
		<p>
			<%= csv_icon_link(	general_election_english_region_uncertified_candidacy_list_url( :format => 'csv' ), "Download the data shown on this page: uncertified candidate data in England, #{@english_region.name} as a CSV file." ) %>
		</p>
		<p>
			<%= csv_icon_link(	general_election_english_region_candidacy_list_url( :format => 'csv' ), "Download the full data set: candidate-level general election result data in England, #{@english_region.name} as a CSV file." ) %>
		</p>
	</div>
<% end -%>