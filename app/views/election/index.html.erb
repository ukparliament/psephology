<div id="general-elections">
	<%= content_tag( 'h2', 'General elections' ) %>
	<ol>
		<%= render :partial => 'general_election/general_election', :collection => @general_elections %>
	</ol>
</div>

<% unless @parliament_periods_with_by_elections.empty? -%>
	<div id="by-elections">
		<%= content_tag( 'h2', 'By-elections' ) %>
		<ol>
			<%= render :partial => 'parliament_periods_with_by_elections', :collection => @parliament_periods_with_by_elections %>
		</ol>
	</div>
<% end -%>

<% unless @notional_general_elections.empty? -%>
	<div id="notional-results">
		<%= content_tag( 'h2', 'Notional results – adjusted for boundary changes' ) %>
		<ol>
			<%= render :partial => 'general_election/general_election', :collection => @notional_general_elections %>
		</ol>
	</div>
<% end -%>
	

<div id="downloads">
	<p>
		<%= csv_icon_link( general_election_list_url( :format => 'csv' ), "Download general election data as a CSV file.") %>
	</p>
</div>