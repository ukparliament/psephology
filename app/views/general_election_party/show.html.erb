<%= render :partial => 'previous_next_show', :object => @general_election unless @general_election.is_notional %>

<%= render :partial => 'general_election/counts', :object => @general_election %>

<% if @general_election.has_results? -%>
	<ul>
		<%= content_tag( 'li', link_to( 'Elections won', general_election_party_election_won_url ) ) %>
		<%= content_tag( 'li', link_to( 'Elections contested', general_election_party_election_list_url ) ) %>
	</ul>
<% end -%>

<%= render :partial => 'notional/notice' if @general_election.is_notional %>